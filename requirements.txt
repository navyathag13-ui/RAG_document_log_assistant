fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
chromadb>=0.4.22
sentence-transformers>=2.2.2
pypdf>=3.17.0
python-multipart>=0.0.6
openai>=1.3.0
rank-bm25>=0.2.2

# Azure OpenAI Service (Phase 1) — uses the openai package's AzureOpenAI
# client, no separate SDK needed; openai>=1.3.0 above already covers it.

# Agent orchestration (Phase 2)
semantic-kernel>=1.18.0

# Responsible AI safeguards (Phase 3)
azure-ai-contentsafety>=1.0.0
azure-core>=1.29.0

# Azure Monitor / Application Insights telemetry (Phase 4)
azure-monitor-opentelemetry>=1.6.0
